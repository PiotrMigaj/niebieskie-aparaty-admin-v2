# Optional dev-only override. In production builds the app reads encrypted
# credentials from the OS keychain instead and ignores this file.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-central-1
DDB_TABLE_NAME=niebieskie-aparaty-prod
